# Sync client target. Leave UNSET for same-origin (one Cloudflare Pages deployment serves both the app and the /v1/blob sync API); set a full URL to point elsewhere, or set it to "" to disable sync (local-only). Public value, not a secret.
VITE_SYNC_URL=
